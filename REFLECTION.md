--- REFLECTION ---

1. The predictions that were answered with my functions were correct, which makes sense. I have access
   to the data because I'm writing the code, so I literally know the answers before the computer can
   calculate to find the same result.

2. It was surprising how quickly I was able to start using 'let ___ in ___' for my for loops. When
   looking at them in the tracker file, specifically in the mostReadBook function, I thought it'd take
   me a while to get used to the notation, but actually its pretty simple to understand once you're
   writing with it.

3. The correlation function was difficult because I had to incorperate the functions provided by the
   textbook. I just copy-pasted the actual formula for the phi coefficient, since that part felt
   fine to do that with since it's just a math formula. I had to write down with pencil and paper a
   truth table for caffeinated/focused and write a loop to make a table representation with an array
   myself, because the way that function was written in the textbook wasn't a 1:1 representation for
   what I needed it to do, but since I understood the principle logic of that function, I was pretty
   easily able to recreate it after writing a table down on paper.

4. I had to spend a while trying to remember accurate data for some keys, like mood and sleepHours, but
   screenTime was simple to find on my phone. The correlation between coffee and focus prompted by the
   professor was a dud with my data, though, since I don't drink or consume any caffeine, meaning the
   correlation function results in Nan, since it tries to divide by 0.
